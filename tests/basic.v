fn main() {
    assert true
    assert !false

    assert 0 == 0
    assert 0 != 1
    assert 0 < 1
    assert 0 <= 1
    assert 1 > 0
    assert 1 >= 0

    assert true && true
    assert true || false
}
